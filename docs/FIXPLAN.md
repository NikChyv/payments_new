# План устранения находок

По отчёту `docs/REVIEW-2026-09-09.md` (ревью по карте `docs/MODULES.md`).
Порядок — по тому, что эксплуатируется снаружи и что дороже стоит, а не по
номерам модулей.

**Отчёт составлен по чтению кода, руками не воспроизведён.** Перед починкой
каждой находки — воспроизвести на стенде, по правилу из `REVIEW-BRIEF.md` (п. 8).
Что уже подтверждено фактом — отмечено ниже.

---

## Этап 0. Утёкший ключ — ✅ ЗАКРЫТО 09.09.2026, вечер

**Статус: подтверждено.** `raw.githubusercontent.com` отдаёт
`supabase/migrations/20260622000000_baseline.sql` без авторизации; в строках 275
и 279 — JWT с `"role":"service_role"`, `"ref":"gmvhphuabiyggfurfhmc"`, срок до
2036 года. В истории с коммита `8df37a3` (24.06.2026).

`service_role` обходит RLS целиком: все заявки, все токены клиентов, `auth`,
Storage мимо лимитов политики, любая RPC. Пока ключ жив, остальные пункты плана
смысла не имеют.

**Проверено 09.09 вечером:** ключ не просто лежит в репозитории — он **живой**.
`GET /rest/v1/clients` с ним отдаёт 200, то есть читает токены всех клиентов, а
по ним открываются их кабинеты.

**Найдено попутно, в отчёте этого нет:** те же триггеры создавались и при
локальном `supabase db reset` — стенд разработчика слал вебхуки боевым функциям
с боевым ключом. Убрано вместе с ключом.

**Сценарий — без простоя.** В проекте уже заведены новые ключи
(`sb_publishable_…`, `sb_secret_…`), подпись JWT переведена на ECC, а legacy
отключается кнопкой. Значит **ротировать JWT-секрет не нужно**: старые и новые
ключи работают параллельно, переводим по одному с проверкой на каждом шаге.

Подготовка — сделано:

- триггеры вебхуков убраны из `baseline.sql` (они и так живут вне миграций,
  `ARCHITECTURE.md` п. 5), вынесены в `scripts/webhooks.sql` с плейсхолдерами;
- chat_id бухгалтеров заменены плейсхолдерами (M11.8);
- функции читают `SB_SECRET_KEY` с откатом на `SUPABASE_SERVICE_ROLE_KEY` —
  работают и до отключения legacy, и после;
- `notify-client` и `notify-payment` проверяют `x-webhook-secret` (закрывает
  0.2), но **только если** `WEBHOOK_SECRET` задан — иначе проверка выключена,
  чтобы деплой можно было сделать до появления секрета в триггерах;
- `supabase/.temp/` в `.gitignore`, снимки `pgdelta` с ключом удалены с диска;
- CI-сторож `.github/workflows/secrets.yml`: JWT, `sb_secret_`, токен бота,
  `Bearer` в миграциях. Проверен в обе стороны — зелёный на текущем дереве,
  красный на старом `baseline.sql`;
- 190 pgTAP-тестов зелёные, локальные триггеры-вебхуки исчезли.

**Выполнено полностью.** Legacy-ключи отключены, утёкший service_role отдаёт
401 на всё — проверено четырьмя замерами (первый прошёл на кэше, поэтому
проверять надо не один раз). Уведомления, кабинет клиента и переписка живы,
ошибок в консоли нет. Подделка вебхука публичным ключом получает 403.

Порядок, которым это было сделано:

1. владелец присылает `sb_publishable_…` → правка `app/js/config.js`, пуш;
2. `supabase functions deploy notify-client notify-payment telegram-bot`;
3. владелец: `SB_SECRET_KEY` = `sb_secret_…` в Edge Functions → Secrets;
4. владелец: `scripts/webhooks.sql` в SQL Editor (подставив ключ и свой
   `openssl rand -hex 32`);
5. владелец: `WEBHOOK_SECRET` в Edge Functions → Secrets — защита включилась;
6. проверка сквозного сценария на тестовой фирме;
7. владелец: **Disable JWT-based API keys**;
8. проверка повторно + `curl` утёкшим ключом должен дать 401.

Через неделю: JWT Keys → Previously used → Revoke старому HS256.

**Из истории git ключ не вычистить** (форки, кэши сканеров). Спасает только то,
что старый ключ станет мёртвым.

**Проверка готовности:** старый ключ отвергается REST; заявка на тестовой фирме
проходит путь «вопрос → ответ → уведомление» целиком.

## Этап 1. Дыры, эксплуатируемые снаружи — ✅ ЗАКРЫТО 10.09.2026

| Находка | Что делаем |
|---|---|
| 0.2 ✅ | **Закрыто 09.09 вечером** вместе с этапом 0: функции проверяют `x-webhook-secret`, подделка публичным ключом получает 403 (проверено) |
| M2.1 ✅ | Валидация схемы `url` в `normalize_files` и `reply_by_token`: только `https://`. Сейчас проверяется лишь непустота, а `esc()` не мешает `javascript:` в `href` — клик по вложению выполняет скрипт в сессии бухгалтера. Плюс тест |
| M4.3 ✅ | `esc()` в `notify-payment` и в `send_daily_reminder` — HTML из полей заявки ломает сообщение |

**✅ ЗАКРЫТО 10.09.2026.** Миграция
`20260910000001_url_scheme_and_html_escape.sql`, тестов стало **206** (было 190).

Что сделано:

- **M2.1.** Одна проверка `is_safe_file_url` — только `http(s)`, без пробелов,
  кавычек и управляющих символов. Стоит в `normalize_files` (подача и правка
  клиентом) и в `reply_by_token` (ответ в переписке — он кладёт файл во
  вложения мимо `normalize_files`). Домен намеренно НЕ проверяем: адрес Storage
  разный в проде, в локальном стеке и внутри контейнера функций, белый список
  хостов сломал бы бота и CI, ничего не добавив против схемы. Во фронте зеркало
  проверки — `safeUrl()` в `app/js/utils.js`, все четыре `href` идут через неё;
  она прикрывает то, что сохранено раньше, и `staff_files`, которые пишутся
  общим `save()` мимо RPC (по-настоящему закроется этапом 2).
- **M4.3.** `esc()` в `notify-payment` (плюс ссылка через `safeUrl`), `tg_esc()`
  в утренней рассылке.
- Текст рассылки вынесен в `daily_reminder_message()` — миграцией. Раньше
  починить его миграцией было нельзя: в теле `send_daily_reminder` лежит боевой
  токен, и `create or replace` затёр бы его плейсхолдером (CLAUDE.md, грабли №2).
  Теперь в оболочке остались только токен и рассылка.

**Тесты проверены на излом:** с отключённой `is_safe_file_url` краснеет 10 из 16
новых проверок — то есть они ловят именно дыру, а не факт «не упало».

⚠️ **Остался ручной шаг владельца.** Пока `supabase/daily_reminder.sql` не
выполнен в SQL Editor с подставленным токеном, утренняя рассылка на проде идёт
по-старому, с неэкранированным HTML. Выполнять ПОСЛЕ `supabase db push`, иначе
функции `daily_reminder_message` там ещё нет.

## Этап 2. Убрать общий `save()` — ✅ ЗАКРЫТО 10.09.2026

**Подтверждено фактом, а не чтением кода.** `node tools/racecheck.mjs`
воспроизводит гонку двух вкладок целиком и печатает обе половины:

```
Старое поведение (общий save):
  ✓ вторая вкладка отметила заявку оплаченной
  ✓ сталый upsert откатил статус обратно в «new» — так и терялась чужая работа

Новое поведение (точечный update с проверкой статуса):
  ✓ вкладка всё ещё показывает заявку новой (состояние сталое)
  ✓ после нажатия «Взять в работу» в базе по-прежнему «paid» — чужая отметка цела
  ✓ бухгалтеру сказали, что произошло
```

Первая половина и есть ответ на «а точно ли теряется»: да, теряется, проверено
на живом стенде. Скрипт остался в репозитории регрессом.

Что сделано вместо `save()` (правило записано в CLAUDE.md, раздел «Запись в
очередь — только точечная»):

- `changeStatusRemote(it, from, to)` — переход с условием
  `.eq("status", <что человек видел на экране>)`. Не совпало — перечитываем
  строку, обновляем экран и говорим человеку **(M1.1)**;
- `updateContentRemote(it)` — правка содержания **без статуса**: форма его не
  показывает и менять не должна **(M1.2)**;
- `attachDocRemote(it, from)` — документ бухгалтера с той же проверкой статуса;
- `insertPaymentRemote(rec)` — новая заявка одной строкой вместо upsert очереди;
- `removeRemote` теперь ждётся и проверяется: раньше ошибка удаления уходила
  молча в консоль, а строка пропадала с экрана.

Отсюда же закрылись **M4.8** (ложное «бухгалтер изменил» — оно возникало, когда
upsert возвращал в базу сталое содержание) и **M10.2** (такое затирание больше
не пишется в журнал как честная правка).

**M7.1 — закрыто в основной части.** Файл из ответа клиента больше не затирается
сменой статуса. Осталось честным компромиссом: правка содержания заменяет набор
файлов тем, что в форме, — то есть файл, приложенный клиентом при открытой
форме, всё ещё потеряется. Это уже осознанное действие одного человека, а не
побочный эффект всей очереди; окончательно лечится версией строки, и делать это
имеет смысл вместе с редизайном формы.

**Заодно закрыт M1.3 из этапа 3.** Копия повторяющегося платежа собиралась
клонированием заявки целиком и тащила за собой платёжный документ бухгалтера и
всю переписку по прошлому платежу. Теперь копия собирается по полям. Чинить это
отдельно позже было бы странно: код всё равно переписывался здесь.

Трогает `supabase.js`, `queue.js`, `main.js`; из `tools/uicheck.mjs` вынесен
общий стенд `tools/stand.mjs` — его же использует `racecheck.mjs`.

## Этап 3. Уведомления и очередь — ✅ ЗАКРЫТО 10.09.2026

Миграция `20260910000002_notifications.sql`, правки в обеих функциях
уведомлений, тестов стало **222** (было 206).

**M4.1 — ветки больше не запирают друг друга.** Каждая ветка `notify-client`
заканчивалась `return`, и первая сработавшая отменяла все остальные: у клиента
без Telegram ветка «документ» входила и выходила на каждом UPDATE, а «оплачено»
не доходило никогда. Теперь ветки складывают результат в общий `patch`, строка
обновляется один раз в конце — заодно это один повторный вызов вебхука вместо
нескольких.

**Проверено на стенде, и по ходу нашлось то, чего в отчёте не было.**
`tgDocument` обрабатывал «файл отдал 404», но не обрабатывал «файл не
отозвался»: `fetch` в этом случае **бросает**, исключение вылетало из ветки и
рубило весь вызов. То есть недоступное хранилище отменяло не одну ветку, а все
сразу — та же болезнь, только через исключение. Теперь ни `tg`, ни `tgDocument`
бросать не могут, а накопленные флаги записываются даже на исключении (иначе
при следующем касании заявки клиент получил бы те же сообщения второй раз).

Проба на локальном стенде, битая ссылка на документ плюс переход в «оплачено»:

```
[{"branch":"документ","detail":"файл не забрать: tcp connect error…"},
 {"branch":"статус",  "detail":"401 Unauthorized: invalid token specified"}]
```

Ветка «документ» упала, ветка «статус» **всё равно отработала** — раньше она бы
не выполнилась вовсе.

**M4.4 — отказы больше не растворяются.** Таблица `notify_failures` (RLS без
политик: снаружи не читает никто), обе функции пишут туда каждую осечку с
веткой, заявкой, чатом и ответом Telegram. `health.yml` сторожит счётчик через
`notify_failures_recent` — она отдаёт **только число**, поэтому её зовёт
публичный ключ фронта и новых секретов в Actions не нужно.

Попутно: `notify-payment` читал `TELEGRAM_CHAT_ID` через `!`, то есть обещание
компилятору вместо проверки. Пропади секрет — функция падала прямо на импорте,
не успевая ничего записать. Теперь это отдельная запись в журнал.

**M4.5 — личные задачи.** У сообщения появился адресат: `daily_reminder_message
(staff_id)` кладёт в письмо заявки клиентов плюс собственные личные задачи
получателя. Чужих личных задач не видит никто; в запасном письме по зашитым
chat_id их нет вовсе.

**M4.6 — получатели из `staff.telegram_id`**, по письму на человека (иначе
личные задачи не разделить). Пока колонка пуста, работает прежний путь по
зашитым chat_id; заполнит Никита — запасной путь выключится сам.

**M1.5 — «документ отправлен» только если документ просили.** У заявки с
`need_receipt = false` кнопка «Закрыть» слала клиенту «📄 Платёжный документ
отправлен», и он шёл искать документ, которого не было.

⚠️ **Ручные шаги владельца** (после `db push`):
1. `supabase/daily_reminder.sql` в SQL Editor с токеном — как и в этапе 1;
2. проставить `staff.telegram_id` всем троим, иначе Татьяна по-прежнему не
   получает рассылку, а личные задачи не попадут никому.

## Сверх отчёта. Адресные уведомления бухгалтерам — ✅ 14.09.2026

В отчёте ревью этого нет — всплыло, когда подключали ещё одного бухгалтера.
В приложении каждый видит только своих клиентов, а в Telegram «новая заявка»,
«клиент ответил» и «клиент изменил» уходили **всем** номерам из секрета
`TELEGRAM_CHAT_ID`, утреннее письмо тоже содержало всех клиентов. С третьим
человеком уведомления превратились в общую корзину.

Миграция `20260914000001_addressed_notifications.sql`, тестов стало **239**.

- `notify_staff_chats(client_id)` — одно правило на обе функции уведомлений:
  бухгалтер клиента плюс админы, без повторов; клиент без бухгалтера — админам;
  у бухгалтера нет Telegram — админам, а функция пишет это в `notify_failures`.
- `daily_reminder_message(staff_id)` — бухгалтеру только его клиенты, админу все.
- Номера — только `staff.telegram_id`, секрет `TELEGRAM_CHAT_ID` функциями
  больше не читается (второе место, где номера уже разъезжались, M4.6).
- Попутно в `notify-payment` закрыт тот же баг, что в этапе 3 в `notify-client`:
  необёрнутый `fetch` при обрыве сети бросал мимо журнала.

Проверено на стенде с фальшивым токеном бота: каждая отправка срывается и
пишется в журнал **с номером чата**, так что видно, кому функция пыталась слать.
Заявка от клиента бухгалтера `222` ушла в `111` (админ) и `222` — и никуда
больше; от клиента бухгалтера без Telegram — только `111` плюс запись «у
бухгалтера не указан telegram_id».

## Этап 4. Бот и права — ✅ ЗАКРЫТО 14.09.2026

Миграция `20260914000002_payment_ownership_and_delete_snapshot.sql`, правки в
боте, тестов стало **251**. В тот же заход взяты M5.3 и M6.7: первое живёт в том
же файле бота, второе — десять минут.

**M5.1 — чужой `/start` больше не отвязывает молча.** Перепривязку фирмы к
другому Telegram не запрещаем (сменить телефон или передать фирму коллеге —
законно), но прежний владелец получает предупреждение: «фирма привязана к
другому аккаунту, если это не вы — сообщите бухгалтеру». Утечку ссылки теперь
заметят за минуту, а не через месяц. `bind_test.sh` дополнен сценарием, все 8
проверок проходят; в логе функции видно, что предупреждение уходит прежнему
владельцу раньше, чем «Готово» новому.

**M5.4 — ошибка не стирает черновик.** Раньше сессия чистилась до проверки
результата, и лимит частоты или временная ошибка уничтожали заявку, набранную за
восемь шагов. Теперь черновик остаётся на экране подтверждения. Проверено на
стенде: заявка на субботу отвергнута сервером, черновик на месте, лишней заявки
нет.

**M5.3 — ответы бота экранируются, отправка не бросает.** `esc()` на всех
пользовательских полях (получатель, реквизиты, назначение, имя файла, фирма);
`send()` смотрит `res.ok` и не роняет апдейт при обрыве сети. Отдельно:
`answerCallback` зовётся ДО обработки нажатия, и необёрнутый `fetch` там съедал
само действие — «Подтвердить» не создавало заявку.

**M6.3 — заявку клиента не увести в личные.** Триггер
`guard_payment_ownership`: `client_id` и `created_by_staff` меняет только админ.
Служебные пути (`service_role`, SQL Editor, каскад `ON DELETE SET NULL`) не
ограничены, а фронт эти колонки после создания не пишет.

**M6.4 — удаление оставляет снимок строки** в `payments_audit.changes.deleted`.
Запрещать удаление не стали: убрать ошибочную заявку — обычная работа
бухгалтера.

**M6.7 — `supabase-js` на точной версии с SRI.** Проверено в браузере: с
испорченным хешем Chrome блокирует файл. Подробности и порядок обновления —
`SECURITY.md` п.5.6.

Все новые тесты проверены на поломке: без триггера и снимка краснеют 6 из 12.

**M6.6 — регистрация в Auth — ✅ закрыто 14.09.** Обнаружено, что на проде она
открыта. Данные за ней не лежали (проверено: RLS и внешний ключ), но это
переключатель в панели — владелец выключил в тот же вечер, снаружи
`/auth/v1/settings` отдаёт `disable_signup: true`. `SECURITY.md` п.5.7.

## Жёлтые, которые в этапы не попали

Отчёт разложил по этапам не всё. Эти 🟡 никуда не вошли — держим здесь, чтобы не
потерялись. По каждой перед починкой — воспроизвести.

| Находка | Суть | Чем опасна |
|---|---|---|
| **M1.6** ✅ | `max_rows = 1000` — очередь молча обрежется | На 16.09 заявок **меньше 200** — не горит. Пагинацию не делали; очередь теперь предупреждает, когда загрузилось 1000 строк |
| M1.4 ✅ | отмена оплаты удаляет «похожую» заявку по совпадению полей | Копия несёт `parent_id`; удаляется по нему и только нетронутая — условие в самом `delete` |
| M2.2 ✅ | сервер не проверяет сумму и длину полей | Триггер `validate_payment_fields` на все двери; бот и форма говорят о пределе сразу |
| M2.3 ✅ | «Редактировать» на заявке, заведённой бухгалтером | Кнопки нет |
| M2.4 ✅ | «сегодня после 17:00» при правке молча сдвигает дату | Нетронутая дата не переносится; форма клиента и бот называют настоящую дату |
| M3.2 ✅ | heic на Windows даёт неверную причину отказа | Тип файла передаётся бакету явно по расширению |
| M5.2 ✅ | групповой чат привязывается как личный | Решение владельца 16.09: `/start` вне личного чата отказывает. Уже привязанные группы не трогаем — найти их: `select name, telegram_id from clients where telegram_id < 0;` |
| M7.2 ✅ | вопрос по закрытой заявке задать можно, ответить — нет | Решение владельца 16.09: вопрос по `sent` запрещён |
| M7.3 ✅ | нет общего потолка на `files` после ответов | `reply_by_token` считает вместе с уже приложенными, текст проходит всегда |
| M9.1 ✅ | `addClient` не проверяет, что `staff_id` — сотрудник | FK это и так держал; настоящая дыра была — сменить бухгалтера негде. Админу — «Изменить» |
| M9.2 ✅ | название клиента денормализовано в `payments.client` | `update_client` переписывает имя в заявках, вебхуки на это время выключены |

**✅ Закрыто 16.09.2026.** Миграция `20260916000001_yellow_findings.sql`,
тестов стало **286**. Решения владельца 16.09: вопрос по закрытой заявке
запрещён; новые привязки бота к группам запрещены; админу обе кнопки — «переименовать» и «сменить бухгалтера»;
нетронутую дату не переносим, о переносе предупреждаем.

Проверено на излом: без миграции краснеют все проверки полей; с
`update_client` без выключения вебхуков — оба теста на очередь `pg_net`; со
старой `edit_payment_by_token` — тесты даты.

**Найдено по ходу, в отчёте не было.** Опасность хуже, чем в M9.2: прямое
переименование переписывает `payments.client` во всех заявках, а `notify-client`
на любом UPDATE «допосылает» недошедшее — клиент, недавно привязавший бота,
получил бы пачку старых документов и вопросов. Поэтому `update_client` выключает
вебхуки на время записи (правило в CLAUDE.md).

**Второе найденное.** Утреннее письмо собирается одним сообщением, а у Telegram
предел 4096 символов: админ со всеми клиентами на ~20 заявок с назначением и
файлом упирался в него, и письмо не уходило целиком — молча, мимо
`notify_failures`. Теперь назначение в письме режется до 150 символов, а
заявки сверх ~3500 символов считаются строкой «…и ещё N».

**Мой откат M11.8, для честности:** в комментариях двух миграций (10.09 и 14.09)
остался реальный `chat_id` Никиты. Без токена бота он бесполезен, а из истории
git его уже не убрать — править применённые миграции ради этого не стали.

## Этап 5. Редизайн и оплата по частям

**Только после этапа 2.** Рефакторинг перепишет ровно те функции, поверх которых
ляжет новая вёрстка; сделаем раньше — переделывать дважды. План редизайна —
`docs/REDESIGN.md`, решения по частям — в переписке владельца с ассистентом.

## Этап 6. Инфраструктура — фоном

M11.4 — **branch protection**: сейчас `git push` в `main` выкатывает фронт на
Pages **до** того, как отработает `test.yml`; красный тест приходит, когда уже
поздно. Минимум — required check `DB tests` и работа через PR.

M11.3 (один полный список ручного восстановления вместо разбросанного по 6.x),
M3.1 (файлы Storage не бэкапятся вообще), M11.7 (легаси-SQL рядом с миграциями —
перенести в `legacy/`).

~~M11.5~~ (`health.yml` не сторожит доставку уведомлений) — ✅ закрыт этапом 3:
журнал `notify_failures` и проверка счётчика в `health.yml`.

Остальное 🔵 — попутно с профильными задачами.

---

## Понижено после проверки

**M6.5** — `GRANT ALL … TO anon` на `clients`, `staff`, `tg_sessions` выглядит
страшно, но RLS включена на всех трёх, а политик для `anon` нет: аноним получает
пусто. Гигиена, а не дыра. Чистить попутно.

## Чего в отчёте нет, а держать в голове надо

- **Тестовая фирма с заявкой висит на проде** — после ротации ключа проверяем на
  ней, что уведомления живы.
- **PDF руководства клиента устарел**: `docs/client.md` изменился 09.09,
  а PDF собирается руками.
